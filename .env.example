# Where the /news page fetches its data from.
# - Local dev / Vercel build: leave empty -> fetches this app's own /api/news (same origin).
# - GitHub Pages static build: set to the deployed Vercel app's news API, e.g.
#     PUBLIC_API_BASE=https://your-app.vercel.app/api/news
PUBLIC_API_BASE=
