# The domain the /news page's API calls (/api/news, /api/read) go to.
# - Local dev / Vercel build: leave empty -> same-origin calls.
# - GitHub Pages static build: set to the deployed Vercel app's domain (no path), e.g.
#     PUBLIC_API_BASE=https://your-app.vercel.app
PUBLIC_API_BASE=
